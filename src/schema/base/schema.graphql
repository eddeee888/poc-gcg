type Query
type Mutation # Comment this out to avoid runtime error when making the queries to localhost:3000/graphql

type PayloadError {
  error: PayloadErrorType!
}

enum PayloadErrorType {
  NOT_FOUND
  INPUT_VALIDATION_ERROR
  FORBIDDEN_ERROR
  UNEXPECTED_ERROR
}

scalar DateTime
